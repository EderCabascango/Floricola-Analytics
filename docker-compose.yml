services:
  db:
    image: postgres:16
    container_name: floricola_db
    environment:
      POSTGRES_USER: floricola
      POSTGRES_PASSWORD: floricola
      POSTGRES_DB: floricola
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
